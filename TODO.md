# TODO

- [ ] Update `components/LabsIndex.jsx` so lab cards are not links.
  - Remove per-card `<Link href="/labs/[id]">` wrapper.
  - Add inline expand/collapse behavior on click.
  - When expanded, show full lab description content (highlights, timeline, keywords/tags/stack, outcome) inside the card.
- [ ] Verify in dev that clicking a card expands it and no navigation happens.

